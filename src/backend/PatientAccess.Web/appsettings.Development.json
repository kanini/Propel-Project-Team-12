{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=db.dhbgcoscqujsfycytvns.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=Maddy@15092001;SSL Mode=Require;Trust Server Certificate=true;Pooling=true;Minimum Pool Size=1;Maximum Pool Size=20;Connection Idle Lifetime=300;Connection Pruning Interval=10;Timeout=30;Command Timeout=30",
    "_comment_pooler": "Pooler connection (Port=6543) can cause connection exhaustion. Using direct connection (Port=5432) with proper pool settings instead.",
    "_comment_local": "For local PostgreSQL, use: Host=localhost;Port=5432;Database=patientaccess_dev;Username=postgres;Password=postgres",
    "_comment_supabase": "Currently using Supabase PostgreSQL direct connection. Pooling configured: Max 20 connections, 30s timeout, 300s idle lifetime."
  },
  "CorsSettings": {
    "AllowedOrigins": [
      "http://localhost:5173",
      "https://localhost:5173",
      "http://localhost:3000"
    ]
  },
  "RedisSettings": {
    "ConnectionString": "localhost:6379",
    "Enabled": false,
    "_comment": "Set Enabled=true and provide Upstash Redis connection string to enable Redis caching in development. When Enabled=false, application uses database-only fallback mode."
  },
  "EmailSettings": {
    "SmtpHost": "smtp.sendgrid.net",
    "SmtpPort": 587,
    "SmtpUsername": "apikey",
    "SmtpPassword": "your-sendgrid-api-key-here",
    "FromEmail": "dev-noreply@patientaccess.com",
    "FromName": "Patient Access Platform (Dev)",
    "FrontendUrl": "http://localhost:5173",
    "_comment": "Development email configuration. For testing without real emails, consider using Papercut SMTP or MailHog (localhost:1025). Email delivery errors are logged but won't break registration flow."
  }
}