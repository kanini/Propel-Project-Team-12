# User Story - US_056

## Story ID

* ID: US_056

## Story Title

* PHI Encryption at Rest & In Transit

## Description

* As a Security Engineer, I want all PHI data encrypted with AES-256 at rest and TLS 1.2+ in transit, so that the platform protects patient data from unauthorized access and meets HIPAA encryption standards.

## Acceptance Criteria

1. **Given** PHI is stored in PostgreSQL (FR-041), **When** data is written to disk, **Then** AES-256 encryption is applied via Supabase's transparent data encryption and column-level encryption for sensitive fields (SSN, insurance IDs).

2. **Given** any API communication (FR-042), **When** data is transmitted between client and server, **Then** TLS 1.2+ is enforced with HSTS headers and no downgrade to HTTP is permitted.

3. **Given** inter-service communication (NFR-003), **When** the backend communicates with external services (Azure OpenAI, Twilio, SendGrid), **Then** all connections use TLS with certificate validation enabled.

4. **Given** the zero-PHI caching strategy (NFR-004), **When** data is cached in Upstash Redis, **Then** only non-PHI session tokens and aggregate data are stored; no patient names, medical records, or identifiers are cached.

## Edge Cases

* What happens if TLS certificate renewal fails? The system alerts ops via monitoring and rejects connections rather than falling back to unencrypted transport.
* How are encryption keys managed? Keys are managed via environment variables in deployment secrets (Railway/Render), never committed to source control.

## Traceability

### Parent Epic

* Epic: EP-010

### Requirement Tags

* FR-041 (AES-256 encryption at rest)
* FR-042 (TLS 1.2+ encryption in transit)
* NFR-003 (Data protection standards)
* NFR-004 (Zero-PHI caching strategy)

### Dependencies

- None

## Visual Design Context (UI Stories Only)

N/A — Infrastructure/security story.

## Story Points: 3
